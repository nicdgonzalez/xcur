# Xcursor

Parser for the Xcursor file format.

## Usage

> [!WARNING]\
> This project is still a work in progress.

Use cargo to add the library to your project:

```bash
cargo add --git https://github.com/nicdgonzalez/xcur.git
```

## Examples

```rust
use std::fs::File;
use std::error::Error;

fn main() -> Result<(), Box<dyn Error>> {
    todo!()
}
```
